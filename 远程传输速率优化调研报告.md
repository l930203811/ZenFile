# 远程传输速率优化调研报告：参考 MaterialFiles 的可行性分析

> 调研对象：`D:\Xiangmu\MaterialFiles-master`（Material Files，原生 Android 文件管理器）
> 目标项目：`D:\Xiangmu\ZenFile-main`（ZenFile，Flutter 文件管理器）
> 调研日期：2026-07-28
> 结论：MaterialFiles 的“近满带宽 + 串流外部播放器”**可以借鉴**，但需按其底层约束改造，不能 1:1 照搬。

---

## 1. 结论先行（TL;DR）

| 技术点 | MaterialFiles 做法 | ZenFile 现状 | 是否可借鉴 | 优先级 |
|---|---|---|---|---|
| 1MB 大缓冲顺序读 | `AbstractFileByteChannel` 1MB 预读 + 异步预取 | SMB 原生层**已是 1MB**（SmbService.kt:1262）；FTP/SFTP/WebDAV 偏小 | 部分已具备 | 中 |
| 异步预读双缓冲（消费 N 时后台拉 N+1） | 协程 `Future` 预取，单连接填满带宽 | Dart 单 isolate 无法真正并行预读 | **不可 1:1**，用“并发 Range 读窗口”近似 | 高 |
| SMB/FTP 连接池 + 长连接复用 | SMB 单会话缓存；**FTP 显式连接池** | SMB 原生会话已缓存；FTP 单连接无池 | FTP 加池可借鉴 | 高 |
| 流式播放 | **FUSE ProxyFileDescriptor** | 已有**本地 HTTP Range 代理**（RemoteStreamingService） | 已采用等价方案 | — |
| SMB3 多通道 | **未使用** | smbj 0.13.0 未实现 | 不建议追 | 低 |

**最关键的一点**：ZenFile 的串流架构（`RemoteStreamingService` 本地 HTTP Range 代理）本身就是 MaterialFiles FUSE 代理在 Flutter 下的**正确等价物**，方向已经对了。真正没跑满带宽的是**喂给代理/落盘的远程读取吞吐**，尤其是 FTP / SFTP / WebDAV 这些纯 Dart 客户端。

---

## 2. MaterialFiles 的核心技术（已逐条核实，附证据）

### 2.1 统一抽象 + 1MB 预读双缓冲（跑满带宽的真正银弹）
- 所有协议统一为 `SeekableByteChannel`，加速逻辑集中在 `AbstractFileByteChannel.kt`。
- 证据：`BUFFER_SIZE = 1024 * 1024`（1MB），`TIMEOUT_MILLIS = 15000`（`AbstractFileByteChannel.kt:295-298`）。
- 机制：内部 `ReadBuffer` 维护一个 `pendingRead: Future<ByteBuffer>`，**消费当前块时，下一块已经异步在飞**（`:205`、`:224-253`）。即双缓冲 / 流水线，让单条连接在等待“当前块落盘”时，下一块已到网卡——这是它单连接就能填满带宽的本质原因。

### 2.2 连接管理（按 Authority 缓存 / 池化）
- **SMB（smbj 0.11.5）**：单 `SMBClient` + 每 Authority 一个 `Session`，长连接复用（`smb/client/Client.kt:52-63`），吞吐靠 SMB3 credits 在同一连接上并发多个读请求。
- **FTP（commons-net 3.13.0）——真正的连接池**（关键可借鉴点）：`clientPool: MutableMap<Authority, MutableList<FTPClient>>`（`ftp/client/Client.kt:41`）；`acquireClient` 用 `sendNoOp()` 探活（`:53`），`releaseClient` 用完归还且**不过期**（`if (false)` 关闭了按时间回收，`:124-131`）。多个读可同时占用池中不同 `FTPClient` → 多数据连接并行 → 易跑满带宽。
- **SFTP（sshj 0.40.0）**：每 Authority 一个 `SSHClient`/SFTP 子系统，长连接缓存（`sftp/client/Client.kt:32`）。
- **WebDAV（dav4jvm）**：每 Authority 一个 `OkHttpClient`（内置连接池：每 host 5 并发 + keep-alive）（`webdav/client/Client.kt:57`）。

### 2.3 数据传输细节
- SMB/SFTP：直接用底层库**异步读**（SMB credits 流水线 / SSHJ async read），`FileByteChannel.onReadAsync`。
- FTP：`RETR` 时 `restartOffset = position`（REST 断点）+ `retrieveFileStream`，按块随机访问（`ftp/client/FileByteChannel.kt`）。
- WebDAV：**HTTP Range 请求** `bytes=start-end`，校验 `206`（`webdav/client/DavResourceCompat.kt:38-60`）。
- 跨协议拷贝走 8KB 流拷贝（`InputStreamExtensions.copyTo`）——这是**慢路径**，非主路径。

### 2.4 流式播放：FUSE 代理（非 HTTP 代理）
- `FileProvider.openFile` → `StorageManager.openProxyFileDescriptorCompat(...)` 把远程 `SeekableByteChannel` 挂成内核可见的虚拟文件（`file/FileProvider.kt:187-212`）。
- 内核回调 `onRead(offset, size, data)` 从远程按偏移取字节（`ChannelCallback`，`:255-366`），任意 seek、边下边播。
- **注意**：这是 Android 原生 FUSE 机制，Flutter/Dart **无法直接用**，必须用“本地 HTTP Range 代理”替代（见 §4.5，ZenFile 已做）。

---

## 3. ZenFile 现状（已逐条核实）

### 3.1 SMB（原生 smbj 0.13.0 via MethodChannel）
- `LanClient` 通过 `MethodChannel('com.sequl.zenfile/smb')` 调用原生 smbj。**与 MaterialFiles 同库族**（版本更新：0.13.0 vs 0.11.5）。
- 关键：`SmbService.kt` 的 `downloadFile` **已用 1MB 缓冲**（`ByteArray(1024 * 1024)`，`:1262`），顺序 `input.read(buffer)` 循环落盘。即“大缓冲”已具备，吞吐天花板高；瓶颈更在“无异步预取重叠”（但 smbj 借 SMB credits 在协议层也算流水线，通常已接近满速）。
- `downloadFile` 在原生线程完成，Dart 侧仅 `Timer` 轮询文件大小做进度（lan_client.dart:350-360）。

### 3.2 FTP（纯 Dart `ftpconnect` 2.0.10）
- 单 `FTPConnect` 控制连接；下载走独立 raw socket（64KB 落盘刷新，ftp_client.dart:463）。
- **无连接池**：每次操作多要重新登录/建链；上传/删除/列表都会 `changeDirectory` 改变 CWD（已修复列表导航，见前序提交 `ed0f286`）。
- 单数据连接 + Dart 事件循环串行，单大文件难以跑满高速链路。

### 3.3 SFTP（纯 Dart `dartssh2`）
- 上传已用 **64 并发写**（`writeBytes` 在途窗口约 4MB，sftp_client.dart:347-374）→ 上传吞吐较好。
- **下载是顺序 `file.read()` 流**（`:254-284`），无并发在途读、无大块预读 → 下载是短板。

### 3.4 WebDAV（Dart `HttpClient`）
- `downloadFile` 用普通 `GET` 流（无 Range）；`downloadRange` 已正确用 HTTP Range（`:336-369`）。
- `getStreamUrl` 直接把**带明文凭据的 URL**交给播放器（`:415-430`）→ 功能可用但凭据暴露，建议收口到本地代理。

### 3.5 串流：已有本地 HTTP Range 代理（方向已对）
- `RemoteStreamingService`：在 `127.0.0.1` 起 `HttpServer`，返回 `206 + Content-Range` 支持 seek（`remote_streaming_service.dart:199-307`）；
- 渐进下载到 partial 文件，100ms 轮询 + 进度回调感知“已落盘字节”（`:551-583`）；
- **按需 seek**：用独立 `seekClient.downloadRange()` 拉取目标字节区间直接输出（`:262-276`、`:62-87`）——逻辑与 MaterialFiles WebDAV `Range` 读取一一对应。
- 结论：**这就是 MaterialFiles FUSE 代理在 Flutter 下的正确等价物，已采用，无需重造。**

---

## 4. 关键架构差异（决定“能否照搬”）

| 维度 | MaterialFiles | ZenFile |
|---|---|---|
| 协议库 | 原生 JVM（smbj/sshj/commons-net/dav4jvm） | 除 SMB 外均为**纯 Dart 单 isolate**（dartssh2/ftpconnect/Dart HttpClient） |
| 并发模型 | JVM 线程池 + Kotlin 协程（真·轻量并行） | Dart 单线程事件循环（async 是协作式，**无法真正并行 Socket 读**） |
| 预读 | 协程异步预取（消费 N 时后台拉 N+1） | Dart 中“async 预取”不提供真并行，除非开 Isolate |
| 串流 | FUSE 内核代理 | 本地 HTTP Range 代理（已等价实现） |

**核心约束**：MaterialFiles 的“协程式异步预读”依赖 JVM 协程的并行 I/O。在 Dart 单 isolate 下，单个 read 循环里 `await` 会串行化网络读与落盘；要做到“真·并行在途读取”，要么把热路径挪到**原生层/Isolate**，要么用**多个并发 Range 读请求**（多个 awaited future 可同时推进）来近似——后者在 Dart 上既可行又简单。

---

## 5. 能否采用？落地建议（按 ROI 排序）

### 5.1【高 ROI】下载/代理喂数据：并发 Range 读“滑动窗口”
- **原理**：在 Dart 事件循环上模拟 MaterialFiles 的“多读在途”。对每个文件维护一个窗口（如 4–8MB），**并发**发起多个 `downloadRange(offset, len)` 请求，谁先到谁拼装落盘/喂代理；单个 await 阻塞时，其它读仍在推进 → 单连接也能填满带宽。
- **依据**：SMB/SFTP/WebDAV 都已具备 `downloadRange`（REST/SSH_FXP_READ/HTTP Range）；SMB 的 `downloadRange` 走原生（SmbService.kt:1289+），天然离主 isolate。
- **适用**：FTP（REST 偏移）、SFTP（`read(offset,length)`）、WebDAV（Range）、SMB（原生 range）。FTP 因单控制连接，配合 §5.3 连接池更佳。
- **改造点**：把 `RemoteClient.downloadFile` 的“顺序流”重构为“滑动窗口并发 Range 读”，并让 `RemoteStreamingService` 的 `_doDownload` 也用它（目前是单流顺序下载到 partial）。

### 5.2【高 ROI】统一本地 HTTP Range 代理（已基本具备，补完）
- 把 **SMB/SFTP/FTP/WebDAV 全部**接到 `RemoteStreamingService`；WebDAV 当前走“直链+URL 凭据”（`getStreamUrl`），建议也改为本地代理以避免凭据暴露、统一 seek 逻辑。
- 代理内部已正确实现 206/Range/按需 seek，只需把底层读取换成 §5.1 的滑动窗口读取，串流吞吐即同步提升。

### 5.3【高 ROI】FTP 控制连接池（Dart 侧）
- 仿 MaterialFiles `ftp/client/Client.kt:41`：维护 `Map<Authority, List<FTPClient>>`，`acquire` 时 `sendNoOp()` 探活、用毕归还、不计时销毁；多文件并发与“并发 Range 读窗口”（§5.1）可分别占用不同控制连接 → 多数据连接并行。
- 收益：多文件并发明显；单大文件受单数据连接+事件循环限制，仍需 §5.1 的窗口读来补。

### 5.4【中 ROI】SMB 维持原生，补可选异步预取
- SMB 已是原生 smbj 且 1MB 缓冲已就位，吞吐本不弱。若实测仍不满，可在 `SmbService.kt` 用 Kotlin 协程做“读 N 时后台预取 N+1”（即把 MaterialFiles `AbstractFileByteChannel` 的预读逻辑搬到原生层）——这是 Dart 做不到、但原生层可完美复刻之处。

### 5.5【中 ROI】WebDAV 大块读 + 显式 keep-alive
- Dart `HttpClient` 自带连接池；主要确保大块读取、不过度分片，必要时复用同一 `HttpClient` 实例并开启 keep-alive（当前每次 `connect()` 新建 `HttpClient`，`disconnect()` 即关闭 → 可改为按连接缓存复用）。

### 5.6【低 ROI / 不建议】SMB3 多通道（Multichannel）
- MaterialFiles **全仓未配置**多通道（已确认无 `withMultiChannel`/`MultiChannel`）。smbj 0.13.0 亦未实现多通道（见项目记忆）。实现成本高、单流收益有限。**优先做 credits 流水线 + 预读/窗口读。**

---

## 6. 风险与权衡
- **Dart 单 isolate 上限**：纯 Dart 协议（FTP/SFTP/WebDAV）的读取无法像 JVM 协程那样真并行；§5.1 的“并发 Range 读窗口”是性价比最高的近似。极端追求时，可把某协议整个热路径挪到 MethodChannel 原生层（如 SMB 已这么做），但成本显著高于窗口读。
- **内存/起播延迟**：大预读/大窗口（1MB+、4–8MB 窗口）会提高首字节后吞吐，但会略微增加内存与拖动进度条时的起播延迟；MaterialFiles 用 15s 超时 + 1MB 是合理的折中，可沿用。
- **安全性**：不要再把凭据写进 URL（WebDAV 现做法）；统一经本地代理（仅 loopback 监听）。
- **兼容性**：SFTP 个别服务端对超大单次读返回异常 EOF（ZenFile 已踩坑，sftp_client.dart:250-253），窗口读应控制单块大小（如 256KB–1MB）并保留分页。

---

## 7. 一句话总结
**可以借鉴，且 ZenFile 的本地 HTTP Range 代理已是对的方向；真正要补的是“并发在途读取”——给 FTP/SFTP/WebDAV 下载与串流代理喂数据加上“滑动窗口并发 Range 读”（近似 MaterialFiles 的多读流水线），并给 FTP 加控制连接池。SMB 维持原生 + 1MB 即可（必要时在原生层补协程式预取）。不要追 SMB3 多通道——MaterialFiles 自己都没用。**

---
### 附：关键证据文件
- MaterialFiles：`app/build.gradle`（依赖版本）、`provider/common/AbstractFileByteChannel.kt`（1MB 预读）、`provider/ftp/client/Client.kt`（连接池）、`provider/smb/client/Client.kt`、`provider/webdav/client/DavResourceCompat.kt`（Range）、`file/FileProvider.kt`（FUSE 代理）
- ZenFile：`android/app/src/main/kotlin/com/sequl/zenfile/SmbService.kt:1262`（1MB 缓冲）、`lib/services/remote/{ftp_client,sftp_client,webdav_client,lan_client}.dart`、`lib/services/remote_streaming_service.dart`（本地 HTTP Range 代理）
